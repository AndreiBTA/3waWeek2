{% extends 'base.html.twig' %}

{% block title %}
    Profile
{% endblock %}

{% block body %}
    {% if(app.user) %}
    <h1>Profile</h1>
    <h3>Change password</h3>
        {{ form_start(form) }}
        {{ form(form) }}
        {{ form_end(form) }}
    {% endif %}
{% endblock %}